[project]
name = "my_t_project"
description = "T pipeline with R analysis and DuckDB anonymization in OrbStack container"

[dependencies]
# Our fixed hello_t package
# my_package = { git = "https://github.com/JohnGavin/hello_t", tag = "v0.1.1" }

[r-dependencies]
packages = ["dplyr", "readr"]

[py-dependencies]
version = "python314"
packages = []

[additional-tools]
packages = ["quarto"]

[t]
min_version = "0.51.2"
